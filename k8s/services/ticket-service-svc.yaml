apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: eventticketing
spec:
  type: ClusterIP
  selector:
    app: ticket-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
