apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: eventticketing
  labels:
    app: booking-service
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
    - name: http
      port: 8080
      targetPort: http
