apiVersion: v1
kind: Service
metadata:
  name: ticket-postgres
  namespace: eventticketing
spec:
  clusterIP: None
  selector:
    app: ticket-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
