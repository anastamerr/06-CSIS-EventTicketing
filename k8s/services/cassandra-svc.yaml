apiVersion: v1
kind: Service
metadata:
  name: cassandra
  namespace: eventticketing
spec:
  type: ClusterIP
  selector:
    app: cassandra
  ports:
    - name: cql
      port: 9042
      targetPort: cql
