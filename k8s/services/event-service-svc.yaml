apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: eventticketing
spec:
  type: ClusterIP
  selector:
    app: event-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
