apiVersion: v1
kind: Service
metadata:
  name: sales-postgres
  namespace: eventticketing
spec:
  clusterIP: None
  selector:
    app: sales-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
