apiVersion: v1
kind: Service
metadata:
  name: neo4j
  namespace: eventticketing
spec:
  clusterIP: None
  selector:
    app: neo4j
  ports:
    - name: http
      port: 7474
      targetPort: http
    - name: bolt
      port: 7687
      targetPort: bolt
